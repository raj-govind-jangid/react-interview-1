export const productList = {
    productList: "products",
    addProduct: "products",
    productsDetail: (id) => { return `products/${id}` },
    productsUpdate: (id) => { return `products/${id}` },
    productsDelete: (id) => { return `products/${id}` },
};